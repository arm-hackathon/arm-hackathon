[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aeolus"
version = "0.1.0"
description = "Deterministic habitat environmental simulation with replayable traces over a user-editable scenario graph (abstract simulation units only)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "numpy>=2.2.6",
]

[project.optional-dependencies]
ml = [
    "onnx>=1.16,<2",
    "onnxruntime>=1.18,<2",
]
dev = [
    "pytest>=8",
    "onnx>=1.16,<2",
    "onnxruntime>=1.18,<2",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
